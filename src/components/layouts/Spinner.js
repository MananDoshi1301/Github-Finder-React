import React from 'react'
import spinner from "./spinner.gif";

const Spinner = () => {
  return (
    <Fragment>
      <img src={spinner} alt="Loading..." srcset="" style={{ width: '200px', margin: 'auto', display: 'block' }} />g
    </Fragment>
  )
}

export default Spinner
